# 小智 AI（字帅简化版）优化报告

> 项目：`xiaozhi_client/`（Flutter 精简客户端，当前版本 2.0.12+14，已发布到 v2.0.11）
> 优化时间：2026-08-05 ｜ 环境：本机未装 Flutter，以下改动均为**源码级静态优化**，未重新编译验证，建议 `flutter analyze` + 真机回归后再出包。

---

## 一、已执行的优化（零/低风险，已落地）

### 1. 清理死代码与无用依赖
| 文件 | 改动 |
|---|---|
| `lib/utils/audio_util.dart` | 删除未使用的 `just_audio` import 与 `_player` 字段；删除未使用的 `collection` 包 import；删除无引用的 `_audioProcessingTimer`、`_tempFilePath` 字段及其 cancel 语句 |
| `lib/services/xiaozhi_service.dart` | 删除未使用的 `flutter_sound` import；删除**无人调用**的 `_handleWebSocketMessage()` 死方法 |
| `pubspec.yaml` | 移除已确认无用的依赖 `just_audio`、`flutter_sound`（减小依赖树体积） |

> 引用核查已通过：`opus_flutter` 仍在用（保留）；`audio_streamer` 在 lib 内无任何引用（见下方建议）。

### 2. 修复每帧空转的死循环（性能）
`lib/services/xiaozhi_websocket_manager.dart` 的 `sendBinaryMessage()` 里有一段**对每个 Opus 音频帧都执行、却从不使用也不打印**的十六进制转换循环。音频帧约每秒 16 个，长期空转浪费 CPU。已删除。

### 3. 防止重连风暴（稳定性）
`XiaozhiWebSocketManager.connect()` 开头补上 `_reconnectTimer?.cancel()`。原先手动连接若与自动重连定时器叠加，可能产生多个并发重连定时器。这与文档里「v2.0.8 修重连风暴」互补，堵住一个仍存在的叠加入口。

### 4. 修复 `AudioUtil.dispose()` 资源泄漏（正确性）
原 `dispose()` 只关闭了音频流控制器，却**没有停止播放器、也没有重置初始化标志**。一旦实例被释放后再重建，`initRecorder()/initPlayer()` 会因 `_isRecorderInitialized/_isPlayerInitialized` 仍为 `true` 而直接跳过初始化，导致二次启动无声。现已在 dispose 里 `stopPlaying()` 并复位标志位。

### 5. 重写过时的安装说明（文档）
`outputs/安装与使用说明.md` 仍停留在 **v2.0.1**，文件名、大小、静音阈值（写着 `SILENCE_RMS_THRESHOLD=600`，源码实为 `minSilenceRms=350` + `noiseMultiplier=2.2`）全部失准，且与《安卓版设置全说明.md》自相矛盾。已按 v2.0.11 真实产物与源码校对重写，并标注 **iOS 仅 v2.0.1、严重落后 Android** 的风险。

---

## 二、建议优化（未执行，需你确认 / 重建验证）

### A. 权限过度申请（隐私 & 上架合规）— 推荐
`audio_util.dart` 的 `initRecorder()` 在初始化麦克风时**一口气申请** `storage`、`manageExternalStorage`、`bluetooth`、`bluetoothConnect`、`bluetoothScan`。其中：
- `manageExternalStorage` 是**危险权限**，Google Play 基本不会批准，且本 App 用 `path_provider`/应用私有目录，根本不需要；
- `storage`/`bluetooth`/`bluetoothScan` 对语音通话并非必需。
建议只保留 `microphone` + `bluetoothConnect`（蓝牙耳机音频路由用），其余移除。

### B. 移除疑似无用依赖 `audio_streamer`
grep 确认 `lib/` 内无任何 `audio_streamer` 引用（录音走 `record` 包）。可从 `pubspec.yaml` 移除。改动前请在全工程（含原生端）再确认一次。

### C. 文档与代码不一致：Dify / MiniMax
两份说明都写「已去掉 Dify、MiniMax、发现页」，但 `lib/screens/chat_screen.dart` 仍**实打实处理** `ConversationType.dify / minimax`（`_initDifyService()`、`MiniMaxService` 等），相关 `dify_service.dart`/`minimax_service.dart`/`*_config.dart` 也都在。二选一：要么真正移除这些分支（顺带瘦身），要么把文档改回「仍支持 Dify/MiniMax」。

### D. 出包产物清理（磁盘）— 推荐，需你授权删除
`outputs/` 现存 31 个 APK/IPA，共 **1804 MB**，其中绝大多数是已被取代的旧版本：
- 建议保留：`小智AI-v2.0.11-arm64/armv7/x86_64.apk` + `小智AI-v2.0.1-iOS-未签名.ipa`（共 191 MB）
- 可清理：**27 个旧包，预计释放 1613 MB**（v2.0.0~v2.0.10 全部、旧的 universal 包等）

> ⚠️ 删除属于破坏性操作，已暂停，等你确认后再执行（会先列出清单、确认后移至回收站而非直接 rm）。

### E. 安装包体积（发布）— 可选
- 手机几乎不用 `x86_64`，可只在发版时保留 `arm64 + armv7`，或改用 **Android App Bundle (.aab)** 由 Play 按需下发，避免一次分发 3 个 APK。

### F. 硬编码魔法数集中化（可维护性）
`RECONNECT_DELAY=3000`、`connect()` 里 200ms 延迟发 hello、`startListeningCall()` 里 500ms 等 session、文档提到的「思考 25 秒超时」「TTS 后 3 秒再听」等散落各处的常量，建议统一收口到 `constants` 文件，便于调参与审阅。

### G. 架构：AudioUtil 上帝类（长期）
`AudioUtil` 用大量 `static` 全局可变状态承载录音/播放/VAD，是此前「RMS 符号解析错」「丢帧」等 bug 的温床。长期可考虑改为实例化管理 + 依赖注入，提升可测试性。当前版本稳定，列为长期项。

### H. iOS 版本同步
Android 已 v2.0.11，iOS 停在 v2.0.1，缺失回声修复、连续对话、断线重连等关键改进。建议在 GitHub Actions 重跑 iOS 构建，保持双端一致。

---

## 三、验证建议
1. `cd xiaozhi_client && flutter pub get && flutter analyze`
2. 真机回归：麦克风录音→静音断句→发送；离线唤醒；语音通话连续对话；断网重连；dispose 后重启不无声。
3. 确认 `outputs/` 清理不影响任何发布/归档需求后再删旧包。

---

## 四、连续对话延迟修复（2026-08-05 第二波 · 针对 v2.0.11「思考太久/不跟手」）

用户反馈：语音通话（连续对话）每轮都感觉「思考时间太长、不像豆包那样自然流畅」。

### 根因：连续对话每轮有两段纯死时间，各约 2 秒
1. **用户说完后** → `silenceTimeoutMs` 默认 `2000ms`，要等满 2 秒静音才发 `listen stop`，服务端才肯开始识别。
2. **小智说完后** → `voice_call_screen` 的 `_relistenDelayTimer` 固定 `2000ms`，小智一停麦克风还要关 2 秒才重新开 → 这 2 秒用户说话直接被忽略，像「卡住」。

服务端在 `manual` 模式下必须等客户端 `listen stop` 才做 ASR，所以这两段等待 100% 是客户端纯死时间，与服务器算力无关。

### 改动（已落地）
| 文件 | 改动 | 效果 |
|---|---|---|
| `lib/utils/audio_util.dart` | `silenceTimeoutMs` 默认 `2000 → 800`；`MIN_RECORD_BEFORE_AUTOSTOP_MS` `600 → 300`（否则会顶掉 <600ms 的低阈值设置） | 用户说完约 0.8s 即断句发送 |
| `lib/utils/audio_util.dart` | `applyVadSettings` 下限 `500 → 300` | 允许设置低至 0.4s 的阈值 |
| `lib/main.dart` | `vad_silence_ms` 读取默认 `2000 → 800` | 首启默认即跟手 |
| `lib/screens/settings_screen.dart` | 字段默认 `2000 → 800`；滑块 `min 1000 → 400`、`divisions 8 → 23`；说明文案改为「想接近豆包就往小写」；`_loadAudioTuning` 读取默认 `2000 → 800`（修掉「一开设置页就被打回 2000」的一致性 bug） | 用户可在 UI 把断句调到 0.4s |
| `lib/screens/voice_call_screen.dart` | `_relistenDelayTimer` `2000 → 400ms`，并在 `assistantSpeakingStop` 时 `await AudioUtil.stopPlaying()` 立刻冲刷 TTS 残音 | 小智一停，麦克风 ~0.4s 后回来；冲刷缓冲防回声串音 |

### 估算收益
每轮死时间从约 **4.0s（2+2）降到约 1.2s（0.8+0.4）**，降幅 ~70%。其中「小智说完后」的衔接从 2s 降到 0.4s，是「像卡住」感的主因，改善最明显。剩余延迟（服务端 ASR+LLM+TTS 首包）属服务器侧，客户端无法再压。

### 仍需你拍板 / 下一步（未执行）
- **开启服务端 VAD（`mode: 'auto'`）**：当前 `startListeningCall()` 用 `manual`，服务端收到 `listen stop` 才识别。若能切到 `auto` 让服务端自己断句并流式触发 TTS，可进一步逼近豆包。但要注意：当前 `xiaozhi_service._onWebSocketEvent` 在「正在录音(_audioStreamSubscription!=null)」时会**丢弃服务端音频**，auto 模式录音常开会导致小智声音播不出来——需同步改造播放闸门（改为「小智在说才播、用户抢话即停」）。此改动涉及 TTS 播放/打断逻辑，风险较高，建议单独验证，本次未做。
- 本机无 Flutter，以上未编译；请用 `flutter analyze` + 真机连续对话回归（重点听：小智停后是否立刻能接话、是否偶发回声串音）。
