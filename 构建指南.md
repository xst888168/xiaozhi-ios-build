# 小智AI 安卓/iOS 构建指南（v2.0.17）

> 本工作区已自带 Flutter / Android SDK / JDK 工具链，可直接出包（见下文「本工作区已就绪的环境」）。

---

## 一、本次已包含的改动（相对 v2.0.16）

修复语音通话「说完话静音满 1 秒仍不发送、卡在『正在说』」：

- **根因**：`lib/utils/audio_util.dart` 的 `startRecording()` 是幂等的（`if (_isRecording) return;`）。语音通话里麦克风只在「打断监听」阶段（小智播报时）以 `onSilenceAutoStop = null` 启动过一次，那时根本没有创建「静音断句定时器」；之后用户开口→打断或助手讲完→自动重听，都会再次调用 `startRecording()`，但此时已在录音 → 直接 early-return → 静音定时器永远没被创建。`onSilenceAutoStop`（真正负责「静音满 1 秒自动发送」的回调）因此永不触发，UI 永远卡在「正在说」。
- **修复**：`startRecording()` 在「已在录音」的分支里改为调用新增的 `_rearmSilenceTimer()`，依据当前 `vadEnabled / onSilenceAutoStop / silenceTimeoutMs` 重新装备静音断句定时器；并把 `_hasSpoken` 重置为 `false`，确保必须检测到本轮真实语音才允许断句发送（避免把助手自己的回声/环境噪声当成「已说话」而误发送）。全新启动路径同样走 `_rearmSilenceTimer()`，逻辑统一、无重复。
- 该 bug 同时影响「打断小智后说话」和「助手讲完自动重听后说话」两种路径，本次一并修复。
- 已出包：`outputs/小智AI-v2.0.17-arm64.apk`（arm64-v8a，仅含 64 位原生库）。

## 二、历史优化（v2.0.16 相对 v2.0.15）

两个语音通话增强 + 锁屏保活：

- **打断小智（barge-in）**：小智播报期间持续监听麦克风（但不发送音频），一旦检测到用户音量明显高于小智自身回声/环境噪声底，立即打断——停止本地播放、向服务端发送 `abort`、切换到聆听并录制用户的话；用户讲完静音满 **1 秒**（`silenceTimeoutMs=1000`）自动发送并等待回应。实现：`lib/utils/audio_util.dart` 新增 `bargeInEnabled` / `bargeInGain` / `onSpeechStart` 打断检测（自适应阈值×增益，避免被小智回声误触发）；`lib/screens/voice_call_screen.dart` 新增插话监听 `_startBargeInMonitor` 与 250ms 二次确认 `_onBargeInDetected`（防止回声/瞬噪误打断）。手动「手掌打断」按钮保留。
- **息屏 / 锁屏 / 退后台通话保活**：进入语音通话即拉起 `CallForegroundService`（`foregroundServiceType="microphone"` + `PARTIAL_WAKE_LOCK`），CPU 息屏不休眠、麦克风持续采集，对话不中断；退出通话自动释放。新增 `lib/services/call_keep_alive.dart`、`android/.../CallForegroundService.kt`，`MainActivity.kt` 新增 `com.lhht.ai_assistant/call` 通道，manifest 声明服务。
- 已出包：`outputs/小智AI-v2.0.16-arm64.apk`（arm64-v8a，仅含 64 位原生库）。

## 三、历史优化（v2.0.15 相对 v2.0.14）

- **语音通话「小智说完后仍卡在『小智正在说话』、麦克风不回来」**：根因在 `lib/services/xiaozhi_service.dart` 的 TTS 状态计数——每收到一句 `sentence_start` 就 +1，但 `sentence_end` 不处理，最后仅一个 `tts stop` 把计数 -1，多句回复时计数永远 >0，导致 `_isAssistantSpeaking` 永久为 `true`，UI 卡在“小智正在说话”且下游「说完后 400ms 自动重听」事件（`assistantSpeakingStop`）根本不触发。改为让 `tts stop` 成为权威复位：无条件归零并触发 `assistantSpeakingStop`，说完后麦克风立即回来、可继续对话。
- 已出包：`outputs/小智AI-v2.0.15-arm64.apk`（arm64-v8a，仅含 64 位原生库）。

## 四、历史优化（v2.0.14 相对 v2.0.13）

三个线上 bug 修复 + 视频通话物体识别落地：

- **Bug1 · 语音唤醒无效（含后台唤醒）**：根因是唤醒词词典 `keywords.txt` 带了 `@显示名` 后缀，导致 sherpa-onnx KWS 匹配失败，已去掉后缀；并补齐原生前台服务保活链路——Dart 层 `WakeWordService` 开启唤醒时通过 MethodChannel 拉起 `WakeForegroundService`（`foregroundServiceType="microphone"` + `PARTIAL_WAKE_LOCK`），命中唤醒词后 `bringToFront` 把 App 拉回前台。已在 `main.dart` / `ConsoleScreen` / `SettingsScreen` 三处唤醒入口统一接入。
- **Bug2 · 语音通话说话毫无反应**：根因是通话模式下「开头静音」会立刻触发 VAD 自动断句，把空音频发到服务器 → 无回复。已加 `_hasSpoken` 守卫，只有真的检测到人声后才允许自动断句（逻辑抽成 `lib/utils/vad_decision.dart` 纯函数并由单测覆盖）；同时进入通话前先 `suspend()` 唤醒服务、退出后 `resume()`，避免两个 AudioRecord 抢麦克风；可视化音量条改为真实 RMS。
- **Bug3 · 视频通话物体识别**：按需求「小智看到我这边的物体进行识别」——视频模式改为本地 `CameraPreview`（后置摄像头优先）+ 端侧 ML Kit 物体检测（`google_mlkit_object_detection`），实时把识别到的标签/置信度叠加在预览上，**不上传任何视频帧**。进入视频模式动态申请相机权限，退出释放相机。
- 新增依赖：`camera ^0.11.0`、`google_mlkit_object_detection ^0.14.0`（版本已按 Dart 3.7.2 锁定，避免更高版本要求 Dart 3.8+）。
- 修复 `AndroidManifest.xml` 与 camera 插件 `WRITE/READ_EXTERNAL_STORAGE` 的 `maxSdkVersion` 清单合并冲突（加 `tools:replace="android:maxSdkVersion"`）。
- 已出包：`outputs/小智AI-v2.0.14-arm64.apk`（71.2 MB，arm64-v8a，仅含 64 位原生库）。

## 五、历史优化（v2.0.13 相对 v2.0.12）

- 聊天页标题改为「小智 AI」，状态显示「空闲 / 已连接」，右上角新增齿轮入口直达「配置中心」。
- 新增「配置中心」页面，还原官方配置中心 UI：当前会话、官方接入（OTA/WS/Token）、语音体验、本地扩展、开发者工具。
- 静音自动发送调整为：默认 **1.0 秒**，后台设置支持 **1~3 秒** 自定义。
- 语音通话页新增「视频模式」切换开关（本期已落地本地物体识别）。
- Android 应用显示名改为「小智AI」。
- 已出包：`outputs/小智AI-v2.0.13-arm64.apk`（56.1 MB，arm64-v8a）。

## 六、历史优化（v2.0.12 相对 v2.0.11）

- 连续对话延迟大幅下降：用户说完后静音断句 `2000ms → 800ms`（设置里最低可到 400ms）；
  小智说完后到重开麦克风 `2000ms → 400ms`，并冲刷 TTS 残音防回声。
- 每轮“死时间”从约 4.0s 降到约 1.2s（估算）。
- 清理了死代码 / 无用依赖（`just_audio`、`flutter_sound`、`collection` 冗余 import 与字段）。
- 修了 `sendBinaryMessage` 每帧空转的十六进制死循环、补了重连风暴防护、`dispose` 资源泄漏。

> ⚠️ 当前仍为「手动 VAD」模式：你可以打断小智，小智不会主动打断你（避免抢话/回声）。全双工 / 服务端 VAD 需后续单独做。

---

## 七、环境要求

- **Flutter SDK**：≥ 3.22（建议用项目 `pubspec.yaml` 里锁定的版本）。
- **Android**：Android SDK（API 34 平台）、**JDK 17**、已配置 `ANDROID_HOME` / `JAVA_HOME`。
- **iOS（仅 macOS + Xcode）**：Xcode 15+、CocoaPods。

---

## 八、Android 构建

```bash
# 1. 进入项目
cd <项目路径>/xiaozhi_client

# 2. 拉取依赖（务必先跑，确认无依赖报错）
flutter pub get

# 3. 静态检查（本编辑环境无法编译，这一步在你机器上很关键，先排雷）
flutter analyze

# 4. 出包（按 ABI 拆分，体积最小，推荐：仅打 arm64，约 71MB）
flutter build apk --release --target-platform android-arm64 --split-per-abi
# 产物：
#   build/app/outputs/flutter-apk/app-arm64-v8a-release.apk   ← 本发型使用的包
#
# 如需一次打全架构（fat apk，体积大），可去掉 --split-per-abi：
#   flutter build apk --release --target-platform android-arm64
#   build/app/outputs/flutter-apk/app-release.apk

# 5. 装到已连接设备 / 模拟器
flutter install
# 或手动：adb install -r build/app/outputs/flutter-apk/app-arm64-v8a-release.apk
```

> 出完后建议把新包按 `小智AI-v2.0.15-arm64.apk` 这类命名放进 `outputs/`。
> 并把 `outputs/安装与使用说明.md` 里的版本/文件名/大小同步更新（旧文档仍可对照）。

> 构建踩坑提示：本机型新增了 `camera` 插件后，其 `AndroidManifest` 会声明
> `WRITE_EXTERNAL_STORAGE@maxSdkVersion=28`，与 App 自身的 `maxSdkVersion=32` 冲突导致
> 清单合并失败；已在 App 的 `AndroidManifest.xml` 用 `tools:replace` 解决。若以后再加会声明同名的第三方库，留意同类冲突。

---

## 九、iOS 构建（需 macOS）

```bash
cd <项目路径>/xiaozhi_client
flutter pub get
flutter analyze
flutter build ipa --release
# 产物：build/ios/ipa/小智AI.ipa（未签名，需你自己的证书/描述文件重签后安装）
```

---

## 十、回归测试重点（真机）

1. **后台语音唤醒**：在设置里打开离线唤醒，回到桌面/锁屏说「小智」仍能唤醒并拉起 App（通知栏出现「小智AI 正在后台聆听」）。
2. **语音通话连续对话**：你说完约 1.0s 自动发送（可在配置中心 1~3s 调），小智回完约 0.4s 麦克风回来；开头停顿不再误断。
3. **视频模式物体识别**：通话页切到视频模式，授权相机后看到本地预览，画面中物体（杯子/人/椅子等）实时出现标签与置信度；退出视频模式相机释放、唤醒恢复。
4. 小智说话时你点手掌按钮能否立刻打断（barge-in）。
5. 是否偶发**回声 / 串音**（若出现，优先把设置里“静音自动发送时长”调到 1.0~1.2s）。
6. 断网/弱网重连后能否自动恢复连续对话。

---

## 十一、常见问题

- `flutter analyze` 报 `just_audio` / `flutter_sound` 找不到：确认 `pubspec.yaml` 已移除这两个依赖且执行过 `flutter pub get`。
- 安卓打包报 JDK 版本不符：本项目需要 **JDK 17**。
- 清单合并失败（`Manifest merger failed ... maxSdkVersion`）：通常是新增的三方库与 App 声明了同名权限但属性不同，在 App 的 `AndroidManifest.xml` 对应 `<uses-permission>` 上加 `tools:replace`。
- Gradle 报 `flutter.bat finished with non-zero exit value 1`：多为**残留 Gradle 守护进程环境不一致**导致，执行 `android/gradlew --stop` 后重试，或用 `GRADLE_OPTS=-Dorg.gradle.daemon=false` 关闭守护进程重建。
- 麦克风/蓝牙权限被拒：检查 `android/app/src/main/AndroidManifest.xml` 权限声明（当前仍申请了 `manageExternalStorage`，上架前建议精简，见优化报告）。
